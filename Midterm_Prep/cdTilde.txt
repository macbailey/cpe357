#include <stdio.h>
#include <string.h>
#include <stdlib.h>
int main(int argc, char* argv[])
{
  char* userName = "mbaile17";
  char* home = "//home/";
  char* full = malloc(sizeof(full)); 
  strcat(full, home); 
  strcat(full, userName);
  printf("%s\n", full);
  return 0;
}
